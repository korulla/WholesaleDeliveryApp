const button = document.getElementById('homeButton');

button.addEventListener('click', function() {
    // Redirect to the target HTML page in another folder
    window.location.href = '../../../index.html';
});